version: "3.8"

services:
  openclaw-gateway:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw-beta
    restart: unless-stopped
    ports:
      - "18790:18789"
    volumes:
      - ./data:/data
      - ./workspace:/app/workspace
    env_file:
      - .env
    environment:
      - OPENCLAW_INSTANCE_NAME=beta
      # ワーカー: 高速・低コストモデルを使用
      - OPENCLAW_DEFAULT_MODEL=anthropic/claude-sonnet-4-5
    security_opt:
      - no-new-privileges:true

  openclaw-cli:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw-beta-cli
    volumes:
      - ./data:/data
      - ./workspace:/app/workspace
    env_file:
      - .env
    environment:
      - OPENCLAW_INSTANCE_NAME=beta
    entrypoint: ["openclaw"]
    profiles:
      - cli
